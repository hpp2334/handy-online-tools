import 'package:flutter/widgets.dart';

class ZipViewerWidget extends StatelessWidget {
  const ZipViewerWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return Text("Zip Viewer");
  }
}
